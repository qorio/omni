package shorty;

message CampaignInfo {
	required string id = 1;
	required string name = 2;
}

message Account {

	required string id = 1; // passport.Service.id
	required double createdTimestamp = 2;
	required string name = 2;

	repeated CampaignInfo campaigns = 3;
}